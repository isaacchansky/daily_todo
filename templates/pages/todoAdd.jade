section.page.add-todo

  h2 Add To Do

  p This form and all behavior is defined by the form view in <code>client/forms/todo.js</code>.

  p The same form-view is used for both editing and creating new users.

  form(data-hook="todo-form")
    fieldset(data-hook="field-container")
    .buttons
      button.btn(data-hook="reset", type="submit") Submit
